using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraVideo : MonoBehaviour {

	// Use this for initialization
	void Start () {
		if (!MetaData.photo) {
			//GameObject.Find ("GIF").GetComponent<GIFVideo> ().StartRecord ();
		}
	}
}